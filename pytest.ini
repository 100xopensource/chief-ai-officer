[pytest]
# The plugin root is on the path so tests import `pipeline.*` the same way the
# CLI does, rather than through a copy installed somewhere else.
pythonpath = plugins/100x-caio
testpaths = tests
addopts = -q
